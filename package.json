{
  "name": "@studydefi/money-legos",
  "version": "1.0.12",
  "main": "index.js",
  "types": "index.d.ts",
  "repository": "git@github.com:studydefi/money-legos.git",
  "author": "Adrian Li <li.adrianmc@gmail.com>",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "yarn clean && tsc --declaration && cp package.json dist && cp README.md dist",
    "build:docs": "yarn build && node scripts/genAddrMainnetOverview.js > docs/overview_mainnet_address.md",
    "serve:docs": "docsify serve ./docs",
    "test": "jest",
    "link": "yarn build && cd dist && npm link",
    "prepublishOnly": "echo \"Error: Don't run 'npm publish' in root. Use 'npm run dist' instead.\" && exit 1",
    "dist": "cd dist && npm publish --ignore-scripts"
  },
  "devDependencies": {
    "@types/jest": "^25.2.1",
    "docsify-cli": "^4.4.0",
    "jest": "^25.3.0",
    "json2md": "^1.7.0",
    "ts-jest": "^25.3.1",
    "typescript": "^3.8.3"
  },
  "prettier": {
    "trailingComma": "all"
  },
  "keywords": [
    "ethereum",
    "abi",
    "defi",
    "defi addresses",
    "defi abi",
    "ethereum abi",
    "ethereum address",
    "money-legos"
  ],
  "dependencies": {
    "@openzeppelin/contracts": "^2.5.0"
  }
}
